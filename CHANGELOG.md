# 更新日志

ViteBlog 从 2026-09-15 开始搭建,一直持续部署(没有版本号),下面按日期倒序记录值得一提的变更。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## 2026-09-26

### 新增

- **oh-my-pi 上手系列 4 篇**(`src/posts/omp-0*.md`):安装与首次会话 → 配置分层、模型角色与审批策略 →
  AGENTS.md、技能与 MCP → 子代理并行与会话管理;按官方 docs 整理,系列内互链。

### 修复

- **代码块行距翻倍**:`<pre>` 里每行 `<span>` 之间夹着 `.join('\n')` 生成的换行文本节点,`white-space: pre`
  下每个换行又多渲染出一个空行 —— 相邻两行实测 52.6px(应为 23.7px),整个代码块高出一倍。现在行间不留
  文本节点;复制按钮相应改成按 `.code-line` 逐行拼回换行(复制出来的代码不变)。

### 更改

- **`npm run cover:ai` 换引擎:千问-图像生成 3.0(阿里云百炼)→ 网关 `gpt-image-2.5-sunburst`**
  (OpenAI 兼容网关 `https://puppyrouter.com/v1`,new-api 系;换别家网关用 `IMAGE_BASE_URL` 覆盖)。
  **破坏性变更**:`DASHSCOPE_API_KEY` / `DASHSCOPE_BASE_URL`(`DASHSCOPE_REGION` 一并作废)改为
  `IMAGE_API_KEY` / `IMAGE_BASE_URL`(BASE_URL 有默认值,通常只需填 KEY);模型没有 seed / negative_prompt /
  prompt_extend:旧的 `--index` 换图玩法取消(重跑即换一张,传了会明确报错),禁字要求以 `Avoid: …` 段
  并入提示词;`size` 默认 `1536x1024`(裁成 1200×630)、`--quality` 按网关文档透传;接口仍是
  `POST /images/generations`(模型页「调用示例」里的 chat 模板是通用样板,实测出图走 images 路由,
  回包为 `data[0].url|b64_json`);令牌分组里没有渠道的模型直接报错,不再白白退避重试。
- **全站 62 篇封面统一用新模型重画**(并发 2 跑了约 17 分钟):旧的 Picsum 照片封面与上一代像素插画被
  同名文件整体覆盖,webp 变体、分享卡与静态页随构建重建(62 张、合计 11 MB、全部 1200×630);
  `public/covers` 那套构建期兜底封面只在文章没写 `cover` 时才用到,现在全站都写了显式封面,
  兜底仅在缺失时由构建自动补齐。
- **封面主角统一换成「胖橘」**:`npm run cover:ai` 的提示词改为「保留各主题场景的道具与动作,把动物演员
  换成一只圆滚滚的橘猫(圆肚、橘色虎斑、奶白嘴爪)」,全站 62 篇封面据此重画(首批 59 篇成功、
  3 篇网络抖动失败后补跑);`hello-vite-blog` 保留人工确认过的那张(逐字节校验)。
- **`npm run check:dist` 的封面断言改成逐篇校验**:以前只数 `public/covers` 的 png 张数,不看 frontmatter
  写的 cover 文件是否真的存在;现在每篇都要能解析到封面(手填路径在 dist 里存在,或兜底 png 存在,外链跳过),
  汇总行也随之改为「手填 X + 兜底 Y」。
- **界面文案统一成像素字**:分享条地址/标签、归档页年份与计数、友链页副标题、文章页统计与「编辑此页」、
  搜索页 meta、站点统计卡数字、键盘快捷键、404 页、页脚 RSS·Atom·Sitemap、脚注编号,以及「生成分享图」
  画布上的标语/日期/地址,全部从 `--font-code`(Fira Code)换成 `--font-body`(像素主题下 = Fusion Pixel);
  代码块、行内代码、代码块右上角工具条仍保留 Fira Code。

## 2026-09-23

### 新增

- **Go 入门到精通系列 7 篇**(`src/posts/go-guide/`):环境与工具链 → 变量与基础类型 → 函数/方法/结构体 →
  接口与错误处理 → 并发(Goroutine/Channel/sync/context)→ 泛型与标准库(含一个完整 JSON API)→
  测试/性能/工程化。每篇 5–9 千字,首尾互链;7 张封面走 `npm run cover:ai`(场景写进 `scripts/cover-scenes.json`)。

### 修复

- **切换页面时约 1 秒的错版**:宽屏下 `.post` 是「目录 + 正文」两栏网格,而目录要等正文 chunk 到位才渲染,
  于是进文章页的头一秒整篇被挤进 220px 的目录列、等正文到了再跳回来(实测 CLS 0.68)。
  现在正文加载期间渲染同宽的**目录骨架**(`.toc-skeleton`,颜色取 `currentColor`,十套主题通用),第一帧版式就是稳定的。
- **跳转意图预取**(`src/data/prefetch.ts`):悬停 / 键盘聚焦 / 手指按下文章链接时,预取该文正文 chunk
  与文章页路由 chunk;页面空闲时再预热文章页 chunk 与首页最新三篇。事件委托挂在 `document` 上,
  十个主题的卡片、归档、标签/分类页、上下篇全都受益。
  实测(300ms 延迟 + 250KB/s 弱网):悬停后点击正文 **138ms** 到位且不再出现「正文加载中…」(此前 857ms);
  冷点击也有骨架兜住版式,CLS 从 0.68 降到 0.06。
- **封面接上 webp 变体**(`src/data/coverVariants.ts`):封面是最重的资源(58 张 1200×630 jpg 平均 111 KB),
  而图片管线早就生成了 480/960 的 webp 变体,只是首页卡片与文章页头图一直直接用原图。
  现在这两处都走变体查表(没有变体的图自动退回原地址):桌面吃 960webp,
  手机吃 480webp —— 首页 5 张卡头的流量从 516 KB 降到 163 KB(**-68%**),
  文章头图 149 KB → 58 KB(**-61%**),手机上再降到 22 KB(**-85%**)。

## 2026-09-21

### 新增

- **`npm run cover:ai`**:用**千问-图像生成与编辑 3.0**(阿里云百炼,OpenAI 兼容接口)按文章主题生成**像素风封面**,
  提示词固定带「8-bit 粗像素 / 深蓝底 + NES 红金青 / 不要文字」,主题短语从标题与标签自动推出;
  并配了中英双语反向提示词(文字/水印/截图/模糊…);`--index N` 换 seed 重打、`--style` 追加风格、
  `--size auto` 交给模型定分辨率、`--extend` 开启提示词改写(默认关)。
  结果裁成 1200×630 落进 `public/images/covers/` 并回填 frontmatter。需要自己提供 `DASHSCOPE_API_KEY`,
  **必须**同时设 `DASHSCOPE_BASE_URL` 为业务空间专属域名 —— 实测 `dashscope.aliyuncs.com` 这类老域名上
  没有 `/compatible-mode/v1/images/generations`(404),缺配置时会直接给出可照做的提示;
  另外 key 支持放进 `.env.local`(已 gitignore),不用写进 shell 历史。


- **全站换真实照片封面**:51 篇文章各配一张 Picsum(Unsplash 图源,免密钥、ID 稳定)的照片,
  裁成 1200×630 存在 `public/images/covers/`,合计 5.1 MB;同一批里不重复使用同一张。
- **`npm run cover`**:给文章找真实照片封面 —— 走 Lorem Picsum(Unsplash 免费照片,免密钥),先人工筛一批 ID 当图池,
  再按 slug 哈希稳定分配(互不重复),裁成 1200×630 落到 `public/images/covers/` 并回填 frontmatter;
  支持 `--all` / `--index N` / `--dry-run`。图片落在 `public/images/` 才能吃到 webp 变体管线,
  而且分享长图是同源取图(远程图会被跨域标脏)。


- **真·全文搜索**:构建期扫全部文章生成 `public/search-index.json`(51 篇,171 KB,正文与代码块都进索引),
  搜索页懒加载后才拉取,加载失败自动退回元信息搜索;结果按 标题 > 标签/分类 > 摘要 > 正文 打分,
  正文命中给「命中词前后各 20 字」的片段并用高亮标出,支持多词全部命中。
  此前 README 写着「全文搜索」但实现只搜元信息,名不副实。
- **图片全部本地化**:正文与封面里 17 张阿里云 OSS 外链图已抓到 `public/images/`(1.6 MB)并改写为站内路径,
  构建期的 webp 变体 + `srcset` 管线这才真正生效(此前 `public/images/` 不存在,管线一直在空转);
  新增 `scripts/import-images.mjs`(幂等、带防盗链头、原图 403 才退回处理版、失败逐条列出并 exit 1)。
- **友链页 `/links`**:卡片墙,数据来自 `site.yml` 的 `friends` 段,支持头像/描述,没配时显示空态;进了 sitemap 与预渲染。
- **文章元信息**:支持 frontmatter `updated`(与 `date` 不同天时文章页显示「更新于 …」,JSON-LD 的 `dateModified` 也用它);
  文末新增「编辑此页 / 报个错」直达 GitHub 源文件与新建 issue。
- **代码高亮扩到 17 种语言**:新增 go / rust / java / kotlin / swift / c / cpp / php / ruby / sql / dockerfile /
  nginx / powershell / ini(toml)/ scss / lua / diff,并补了 tsx / jsx 别名。
- **主题契约可执行化**:新增 `docs/theme-contract.md`(变量契约、优先级规则、新增主题清单、已知的坑)与
  `npm run check:themes`;顺带查出并修好 bento / brutalist 漏定义 `--font-body`、`--font-code` 的漂移。
- **真站点冒烟 `npm run smoke`**:无头 Chrome 打开首页/文章页/标签页/友链页,查渲染、关键元素与控制台报错,
  截图作为 CI artifact;接进 deploy 前的流水线。
- **CI 补齐**:`check:themes`(构建前)、`check:dist`(构建后,校验 head 标签/本地资源/预渲染与图片数量);
  新增 Dependabot(每周分组更新)与 `engines.node >= 24`。
- **社交与分发**:`twitter:card` 四件套、`og:site_name`、`og:image:width/height`、明暗两套 `theme-color`、
  `apple-touch-icon`(构建期从 favicon.svg 光栅化)、`feed.json`(JSON Feed 1.1)。


- **非文章页也预渲染**:`/tags`、`/categories`、`/categories/<分类>`、`/archive`、`/search`、`/about` 现在都有静态 HTML,
  线上返回 200(此前这几类路径是 404,而它们还都写在 sitemap 里);每页有自己的 title / description / canonical。
- **结构化数据与订阅发现**:文章页注入 `BlogPosting`、站点页注入 `WebSite` / `Person` 的 JSON-LD;
  所有页面 head 带 RSS/Atom 的 `rel=alternate`,页脚也加了 RSS · Atom · Sitemap 入口。
- **工程检查进 CI**:`npm run lint`、`npm test`(node 内置 test runner,16 个用例)、`npm run check`
  (内容体检:frontmatter / 标题层级 / 站内死链 / 图片 alt)、主题隔离自检,现在都在 deploy 之前跑。
- **中文像素字体子集化**:`scripts/subset-fonts.mjs` 按站内实际用到的 1538 个字符裁子集,
  588 KB → 47 KB(8.1%),单独成 `fonts-*.css` 异步加载。


- **文章页面内分享条**:复制链接 / 微博 / X,支持系统分享的设备(iOS/Android/部分桌面)会多一个「系统分享」按钮;
  「生成分享图」用 canvas 现画一张带**二维码**的长图(标题 / 日期 / 标签 / 站点信息),下载或长按即可发出去;
  文章带  时封面会通栏铺在标题上方(裁切成 800×380,不变形),图床没开 CORS 时自动退回无封面版并在面板上说明。
- **全文搜索**(导航「搜索」或按 `/`,支持 `?q=` 分享链接)、**分类页 / 归档页 / 分类详情**、
  **标签页增强**(按分类过滤、按数量或名称排序、字号随热度分档)。
- 文章页:**相关文章**(按分类/标签相似度)、**阅读进度条**、**窄屏悬浮目录**、**封面图**(`cover` 字段生效,
  卡片与文章页都用)。
- **代码块增强**:语言标签 + 一键复制 + 行号 + `{2,5-7}` 指定行高亮;**Markdown 扩展**:`::: note` 提示块、
  GFM 风格脚注、带图注的 `<figure>`、外链自动新窗口;**公式与图表**:KaTeX 与 Mermaid 按需加载。
- **图片管线**:懒加载、点击放大(灯箱);本地图构建期生成 webp 变体与 `srcset`(远程图不在清单里,自动跳过)。
- **键盘快捷键**与速查面板(按 `?`);**彩蛋**:Konami 码、首页游戏机卡片的点击粒子。
- 关于页新增「本站数据」面板(篇数 / 字数 / 标签分布,构建期算好,零请求);**404 页**与**随机一篇**。
- **构建期产物**:RSS 与 Atom(`feed.xml` / `atom.xml`)、`sitemap.xml`、`robots.txt`、
  每篇文章的预渲染 HTML(独立 title/description/OG,分享卡片与搜索引擎拿得到)、1200×630 分享图(`dist/og/*.png`)。
- 访问统计接入:GoatCounter 或自建 Umami,`site.yml` 配了才加载(默认不收集任何数据)。
- **一条命令新建文章**:`npm run new -- "文章标题"`(进子目录用 `npm run new -- notes/my-post "标题"`),
  自动生成带 frontmatter 与正文骨架的模板;同名文件已存在时拒绝并提示,不会覆盖。
- **贴边音乐挂件**:固定在左下角,可收成一张封面方块贴边(状态记在 localStorage);支持单曲(`type: song`)与
  歌单(`type: playlist`,播放器多出列表按钮,默认收起),配置在 `site.yml` 的 `music` 段。
  取歌走 Meting 接口 + [APlayer](https://github.com/DIYgod/APlayer),播放器单独成 chunk,不影响首屏。
- **宽屏自适应放大**:从 1800px 起按视口宽度分档给整页 `zoom`,内容占比稳定在 60% 出头,
  2K/4K 屏两侧不再空一大片;≤1799px 保持原始版式。十套主题各按自己的容器宽配了倍率。

### 变更

- 封面风格改为**像素游戏场景**:远景层 + 中景道具 + 地面层,小角色在场景里做事(横版游戏截图构图),
  不再是居中徽章;51 张全部按新风格重生成并逐张目检(无文字,已用视觉模型复核)。


- **首屏瘦身**:`virtual:site-home` 只注入选中的那套首页组件(原先十套首页一起进 entry),文章页改路由懒加载;
  实测首屏 JS 476.7 KB → 331 KB(gzip 149.6 KB → 105 KB)。构建收尾还会删掉 dist 里永远不会被下载的
  woff/ttf 老格式字体(每次约 1 MB)。


- 分享条按钮放大美化:统一 108×40,2px 描边 + `var(--shadow)` 硬阴影(与主题里其它按钮同一套),按下时按钮下沉、
  阴影收掉;复制成功/失败描边走绿/红。
- 分享条去掉「系统分享」按钮(Web Share API 在桌面上多数不可用,留着只是多一个按钮),
  剩下四个按钮统一成 96×28 的最小尺寸;顺带修掉「微博 / X」两个链接继承主题 `a{color:var(--blue)}` 变蓝的问题
  (与分享图面板按钮同一个根因:组件用了 0 优先级的 `:where()`,留了圆角给主题覆盖)。

- 代码块的语言标签与「复制」从代码框上方挪到框内右上角:工具条绝对定位浮在框里(半透明底 + 细边),
  复制按钮不再自带边框。左上方原本独立的一行让出来,代码框整体上移,视觉上工具条成了代码块的一部分。

- 文章正文改为**按需加载**:首页不再内嵌全部 Markdown 原文(此前主包 624 KB,含 51 篇文章全文),
  进文章页才加载那一篇;新增的页面(搜索/分类/归档/404 等)同样按路由懒加载
- 顶部导航新增「搜索 / 分类 / 归档」入口
- 音乐播放器从「关于页里的一张卡片」改成全局挂件:挂在 `App` 上而不是某个页面,切路由不断播;
  固定层小控件(`.back-top` / 挂件)移出 `.page`,不参与宽屏放大。

### 变更

- **全站 51 篇封面换成 AI 生成的像素插画**(`qwen-image-3.0-pro`,深蓝底 + NES 红/金/青,
  与 pixel 主题同一套配色;全部无文字,已逐张目检)。
- `npm run cover:ai` 默认把 `prompt_extend` 打开并给出 `--no-extend`:实测关掉后模型会把主题词
  原样排成标题字(比如给 JavaScript 主题画一张写着 "Jauscript" 的海报),开启后是干净的无字插画。
- 新增 `scripts/cover-scenes.json`:每篇一句纯视觉场景描述("机器人搬木箱"这类,不含任何技术名词),
  脚本优先用它当主题 —— 想重打某篇时最不容易出文字。
- 新增 `--concurrency` / `--delay` 与 429 退避重试(4s→8s→…→90s):账号按请求数限速,
  默认改成串行 + 2s 间隔,`--concurrency 3 --delay 800` 是实测跑通全站的节奏。
- 修掉参数解析 bug:`--model` / `--style` / `--index` 之前会被位置参数的预处理吞掉,
  等于一直用默认值;现在全部生效(`--concurrency 3` 也不会再被当成文章 slug)。
- `DASHSCOPE_BASE_URL` 现在接受裸 WorkspaceId、裸域名、标准 URL、自定义中转四种填法,
  脚本启动时打印补全结果。

### 修复

- **手机上不再显示音量条**:APlayer 只在 `:hover` 时弹出音量条,触屏没有 hover,
  `MusicDock.css` 原本的解法是 `@media (hover: none)` 把它强制成 `height: 40px` 常显 ——
  代价是小屏上一条常驻的红色音量条既占地方又压着正文。现在触屏直接隐藏**整个音量控件**
  (图标一起藏,否则就是个点了没反应的死按钮;APlayer 自身在移动端也是隐藏音量图标的)。
  桌面端 hover 弹出的行为不变,已实测。


- **手机端头部导航压住 logo**:各主题的 `.header-inner` 都是写死高度(pixel 56px、terminal 46px),
  导航换行后高 78px,居中时上下各溢出 11px,第二行直接压在 logo 上。改成「单行 + 横向滑动」
  (规则在 App.css,所有主题共用),能放下就不滚动,桌面端无变化。
- **手机上掌机名片占满整屏**:629px 高的名片在 390×844 屏上只剩一条缝,缩到 `zoom: .86`
  (≤380px 再降到 .78),整块按比例一起缩。
- **固定音乐挂件压住页脚**:展开时约 92px 高的浮层会盖住页面底部,给 body 加上
  124px 底部内边距(用 `:has(.music-dock)` 限定,没配置音乐就不留空白)。


- `npm run cover:ai` 的 `DASHSCOPE_BASE_URL` 现在**认各种填法**:只填 WorkspaceId(`ws-xxxx`)会自动补成
  `https://ws-xxxx.cn-beijing.maas.aliyuncs.com/compatible-mode/v1`(地域可用 `DASHSCOPE_REGION` 覆盖),
  只填域名会补 `https://` 与 `/compatible-mode/v1`,自定义中转的地址原样不动 —— 之前必须一字不差地写全,
  否则报「Failed to parse URL」。解析结果会在启动时打印一行,便于核对。


- `npm run cover` 写进 frontmatter 的封面路径漏了部署 base(`/images/...` 而不是 `/ViteBlog/images/...`),
  文章页 `<img src>` 直接 404、分享图判定成「封面跨域取不到」;顺带给冒烟加了一条「文章头图真的加载出来了」的断言。


- 搜索框的放大镜图标太小:原来用的是 `⌕`(U+2315)字符,Fira Code 里没有这个字形,
  浏览器回退出来的符号只有 9px 宽,在 46px 高的输入框里几乎看不见;换成 20px 的内联 SVG,
  不再依赖任何字体;顺带把清空按钮的点击区从 22px 提到 24px。
  冒烟加了断言(图标 ≥18px + 用只出现在正文里的词能搜到命中),这类问题下次会被 CI 挡下。


- **无障碍**:默认主题 pixel 补上 `prefers-reduced-motion` 兜底(十套里唯一漏的一套,它有三处无限闪烁);
  文章头图此前 `alt=""` 且无宽高(补 alt + 1200×630,消除布局抖动);新增「跳到正文」skip-link;
  浅色主题的 `--muted`(4.25)与 `--green`(2.7,标签文字用)对比度不达标,分别调到 4.92 / 4.68;
  代码块「复制」按钮命中区从 32×19 提到 ≥24px 高。
- 图片本地化暴露出的一处拼接 bug:`pageMeta` 对已带 base 的封面路径会再拼一次,运行期 og:image 变成
  `/ViteBlog/ViteBlog/images/…`;现在三种写法(http / 已带 base / 站内根路径)都能正确拼接。


- 内容体检发现的存量问题:12 张图缺 alt 文字、`typescript入门` 里 3 条 Hexo 时代的站内死链(改成 `/post/typescript/…`)、
  `macd-1` 正文里多出来的一个一级标题(降为二级)。


- 分享图摘录在「一级标题紧跟正文」的文章里摘不出东西(按空行切块时整块被当成标题丢掉),
  改成逐行判断并且整段跳过代码块,13 篇没有 excerpt 的老文章现在 12 篇能自动摘到。
- **音乐挂件音量调不动**:APlayer 的音量条只在  时展开(CSS 里没有触屏兜底),手机上根本叫不出来;
  它的取值又是累加  再减 ,挂件是 fixed 定位,算出来的位置差几像素,拖到底也降不到 0。
  现在由 MusicDock 自己接管音量交互:指针事件(鼠标 / 手指 / 触控笔同一套)、按  换算、
  捕获阶段拦掉 APlayer 自己的拖拽处理,不能悬停的设备让音量条常显。实测拖到顶 、拖到底 
  (以前只能到 0.086 且整体偏 6%),手机触屏拖动同样生效。

- 分享图上的网址会画出卡片外:没有空格的超长词被当成一个整体塞进一行,canvas 直接画到边界之外。
  换行函数现在会把这类词按字符切开,网址在二维码卡片里正常折成两行。

- 分享图面板的「下载图片」按钮文字被染成蓝色:主题里有一条全局 `a{color:var(--blue)}`,而组件样式全用 0 优先级的
  `:where()`,压不住元素选择器。改为普通类选择器并显式重置文字样式(颜色 / 字号 / 字重 / 下划线 / appearance),
  两个按钮同时做成等宽等高的 40px。
- 同一处按钮悬停时冒出下划线:主题的 `a:hover{text-decoration:underline}` 优先级是 (0,1,1),比单个类高一级,
  于是在 hover / active 上再显式声明一遍 `text-decoration: none`。
- favicon 去掉深色底板:原来 7×6 视口被深色矩形铺满,浏览器标签栏里是一圈黑边;重绘为 8×8 透明底像素心形,
  链接加 `?v=2` 绕过 favicon 缓存。
- 贴纸图标清掉抠图残留白边:轮廓上 1-2px 的近白/浅色像素在暗色模式下是一条白边,
  分两轮共清理 90 个文件、1105 个像素(厚度 ≥3px 的浅色图案保留),贴纸 URL 加版本参数。
  (同时按需求删掉了 `sticker-r06-c04`。)
- 窄屏下音乐挂件与「回到顶部」按钮互相遮挡:挂件宽度自动让出一条列,两者始终留 16px 间距。
- 歌单 id 为纯数字时被 YAML 解析成 `number`,原来的配置归一化只收字符串,导致挂件整个不渲染。
- 每页独立 title / description / OG:此前 51 篇文章在浏览器标签、分享卡片与搜索结果里全长一个样。

## 2026-09-20

### 新增

- 关于页**联系卡片**:邮箱 + 社交账号胶囊(数据全来自 `site.yml`),支持 Bilibili 小电视、GitHub、信封、通用地球图标;
  像素主题的头像区直接复用首页那块掌机屏幕。
- 主题补齐到**十套**:新增日式极简「间」、工程蓝图、暗夜霓虹(此前已有 pixel / swiss / editorial /
  brutalist / bento / terminal / glass)。

### 变更

- 主题切换只需改 `site.yml` 一行;构建期只打包被选中的那套,互不混装。
- 补齐本地验证脚本:逐主题构建(`build-all.mjs`)、主题隔离自检(`check-isolation.mjs`)、
  交互控件字号体检(`audit-fonts.mjs`)。
- 交互控件字号统一提升到可读下限(十套主题共 88 处)。
- 站点默认主题切到 `pixel`(红白机像素风)。

### 修复

- 加 `.gitattributes` 统一行尾为 LF(批处理脚本例外保持 CRLF),避免 Windows 工作副本上 lint 必然报错。

## 2026-09-19

### 新增

- 主题体系成型:三套独立主题(pixel / swiss / editorial)由 `site.yml` 切换,
  随后扩到七套(新粗野主义、Bento 便当格、终端 CLI、玻璃拟态)。
- TypeScript 入门到精通系列 7 篇,并支持 TS 代码高亮。

### 修复

- 站内链接自动补部署 base,正文链接改为 SPA 跳转(子路径下不再整页刷新)。
- 修正文档里遗留的 Vue 文案,统一为 React 技术栈。

## 2026-09-18

### 新增

- 标签总览页(带文章数),首页标签过长时截断并给省略号入口。
- 文章系列:JavaScript 从入门到精通(5 篇)、Python 后端开发(5 篇)、
  Node.js 从入门到精通(5 篇)+ Python 后端工具链、AI 编程套餐对比。

### 修复

- 中文标点后加粗无法渲染;暗夜模式下加粗改用金色高亮。
- 柔化暗夜模式文章表格配色。

## 2026-09-17

### 新增

- **24×24 像素表情图标池**(106 个贴纸)接入首页文章卡片。
- 文章页**像素卷轴目录**:羊皮卷配色、卷筒轴头、火漆红高亮。
- 全站「回到顶部」按钮;首页文章列表分页(每页 10 篇)。

### 修复

- 日期显示兼容「带时间」与「仅年月」两种格式。
- 文章详情正文适配屏幕宽度;目录字号与字重调整。

## 2026-09-16

### 新增

- 文章卡片图标按文章哈希随机分配,并扩充像素角色图标。
- Python 入门系列文章。

### 修复

- 子目录下的文章页面未生成;文章按 `date` 字段倒序排序。

## 2026-09-15

### 新增

- 项目初始化:Vue 3 + Vite 博客,支持 Markdown 写作与 GitHub Pages 自动部署。
- **迁移到 React 19 + TypeScript 7**(原生 `tsc` 类型检查),这是项目唯一一次破坏性变更。
- 红白机像素风 UI 主题:按初代 Game Boy(DMG-01)造型重做的游戏机名片、
  本地头像、Fira Code + highlight.js 复古终端配色代码高亮。
- 站点配置抽离到根目录 `site.yml`,由 Vite 插件注入,省略字段自动补全。
- 关于页终端风格小卡片(配色跟随明暗模式)。
- 导入历史文章。

### 修复

- `BrowserRouter` 补上 `basename`,修复子路径下路由不匹配被重定向回首页。
- 页脚 GitHub 链接指向真实仓库;窄屏下导航链接不再换行。

---

## 约定

- 每类变更分 **新增 / 变更 / 修复** 三档,按日期倒序;同一天的同类改动合并成一条,写清「改了哪里、为什么」。
- 涉及 `site.yml` 配置项的改动会点出字段名;破坏性变更单独标注。
